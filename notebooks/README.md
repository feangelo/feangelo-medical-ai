# Notebooks

Notebooks will contain reproducible exploration using synthetic, public, or open-dataset inputs. Each notebook should state its objective, environment, data provenance, expected outputs, limitations, and execution date.

Reusable logic belongs in `src/medical_ai_lab/`, not only in notebooks.

