# Scripts

This directory is reserved for safe operational entry points. Future file-management scripts must provide dry-run behavior, validate all paths, refuse automatic overwrite, and log important operations.

Reusable behavior belongs in `src/medical_ai_lab/` and must be covered by tests.

