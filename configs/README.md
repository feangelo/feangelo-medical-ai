# Shared Configuration

This directory is reserved for versioned, non-secret configuration shared across workflows. Medical-imaging, radiomics, and model configurations will be added only with a documented use case and validation plan.

Never store credentials, private endpoints, local absolute paths, or patient identifiers here.

